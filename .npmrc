# Use official npm registry to avoid mirror issues
registry=https://registry.npmjs.org/

# Disable strict SSL checking if needed (optional)
# strict-ssl=false

# Enable package-lock.json
package-lock=true
